pypdf>=4.0.0
pikepdf>=8.0.0
Pillow>=10.0.0

# Opt-in deps for individual scripts (install only what you need):
#   ocr.py          → pytesseract, pdf2image  (also: system tesseract + poppler)
#   pdf_to_word.py  → pdf2docx
#   html_to_pdf.py  → weasyprint              (also: system pango + cairo on Linux)
# pytesseract>=0.3.10
# pdf2image>=1.16.0
# pdf2docx>=0.5.0
# weasyprint>=60.0
